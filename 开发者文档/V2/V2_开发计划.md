# MemeScan V2 — 多智能体架构升级计划

> **目标**: 将 MemeScan 从"工具流水线"升级为"多智能体协作系统 (Multi-Agent System)"
> **周期**: 5 天 (2026-02-22 ~ 2026-02-26)
> **团队**: 开发者 A (链上交互 & 仿真) + 开发者 B (安全分析 & LLM)

---

## 一、V1 → V2 核心变化

### V1 (当前已完成)

```
MonitorService → SimulationService → AnalysisService → 报告
     (监控)          (仿真)             (规则引擎)
```

- ✅ 单链 (Ethereum Mainnet)、单 DEX (Uniswap V2)
- ✅ 规则引擎评分、蜜罐检测、ownership/mint 检查
- ✅ Streamlit 仪表盘、实时监控、手动扫描
- ✅ MD 报告自动生成

### V2 (本次升级)

```
┌──────────────────────────────────────────────────────────────┐
│                    Coordinator Agent                         │
│               (编排中心 — 接收事件、分发任务、                 │
│                汇总结果、自主决策是否追加检查)                  │
└──────┬──────────┬──────────────┬──────────────┬──────────────┘
       │          │              │              │
  ┌────▼───┐ ┌───▼────┐  ┌─────▼──────┐ ┌─────▼──────────┐
  │Scanner │ │Sandbox │  │  Auditor   │ │   Reporter     │
  │ Agent  │ │ Agent  │  │   Agent    │ │    Agent       │
  │        │ │        │  │            │ │                │
  │·多链监听│ │·买卖仿真│  │·规则引擎    │ │·报告生成       │
  │·BSC    │ │·多额度  │  │·LLM 分析   │ │·Chat 交互      │
  │·ETH    │ │·Anvil  │  │·自主追加检查│ │·风险可视化     │
  └────────┘ └────────┘  └────────────┘ └────────────────┘
```

**核心区别**: Agent ≠ 简单的 Service。Agent 有**自主决策能力** — 能根据中间结果决定下一步怎么做，而不是机械地走固定流程。

---

## 二、各 Agent 详细设计

### 2.1 BaseAgent 基类

所有 Agent 继承统一基类，放在新目录 `agents/` 下。

```
新增文件:
  agents/__init__.py
  agents/base.py          — Agent 基类
  agents/coordinator.py   — 协调者 Agent
  agents/scanner.py       — 扫描者 Agent (封装 MonitorService)
  agents/sandbox.py       — 沙盒 Agent (封装 SimulationService)
  agents/auditor.py       — 审计者 Agent (封装 AnalysisService + LLM)
  agents/reporter.py      — 报告者 Agent (报告生成 + Chat)
```

**`agents/base.py` 核心接口:**

```python
class BaseAgent:
    """所有 Agent 的基类"""
    name: str = "BaseAgent"

    async def run(self, task: dict) -> dict:
        """执行一个任务，返回结果"""
        raise NotImplementedError

    async def decide(self, context: dict) -> str:
        """
        自主决策: 根据当前上下文决定下一步动作。
        返回动作名称，如 "continue", "need_source_analysis", "skip"
        这是 Agent 和普通 Service 的核心区别。
        """
        raise NotImplementedError
```

---

### 2.2 Coordinator Agent — 编排中心

**职责**: 接收 Scanner Agent 的事件 → 分配任务给其他 Agent → 汇总结果 → 做出最终决策

**自主决策逻辑 (核心亮点):**

```python
class CoordinatorAgent(BaseAgent):
    async def run(self, task):
        token = task["token"]

        # 第一步: 让 Sandbox Agent 仿真
        sim_result = await self.sandbox_agent.run({"token": token})

        # 第二步: 让 Auditor Agent 审计
        audit_result = await self.auditor_agent.run({
            "token": token,
            "simulation": sim_result,
        })

        # 🔑 自主决策 — 根据审计结果决定是否追加检查
        action = await self.decide({
            "audit": audit_result,
            "sim": sim_result,
        })

        if action == "need_deep_analysis":
            # Auditor 标记了风险但不确定 → 追加 LLM 源码分析
            deep_result = await self.auditor_agent.run({
                "token": token,
                "mode": "deep_analysis",
            })
            audit_result = self._merge_results(audit_result, deep_result)

        # 第三步: 让 Reporter Agent 生成报告
        report = await self.reporter_agent.run({
            "token": token,
            "simulation": sim_result,
            "audit": audit_result,
        })

        return report

    async def decide(self, context):
        flags = context["audit"]["flags"]
        score = context["audit"]["score"]

        # 决策规则:
        # 1. 如果发现 HIDDEN_MINT 但评分不高 → 需要深度分析确认
        if "HIDDEN_MINT" in flags and score < 50:
            return "need_deep_analysis"
        # 2. 如果仿真正常但 ownership 未放弃 → 也追加分析
        if context["sim"]["can_sell"] and "OWNERSHIP_NOT_RENOUNCED" in flags:
            return "need_deep_analysis"
        return "done"
```

**对应原有代码**: 改造自 `scripts/pipeline.py`，但增加了决策逻辑。

---

### 2.3 Scanner Agent — 扫描者

**职责**: 监听链上事件，发现新代币

**升级点**: 支持多链 (ETH + BSC)

```python
class ScannerAgent(BaseAgent):
    """
    封装 MonitorService，支持多链并行扫描。
    每条链独立的 MonitorService 实例。
    """
    async def run(self, task):
        # task = {"chains": ["ethereum", "bsc"]}
        # 同时监听多条链的 PairCreated 事件
        ...
```

**对应原有代码**: 封装 `services/monitor.py`，外层加多链管理逻辑。

**BSC 配置** (改 `.env` 即可):
```env
# BSC 链 (PancakeSwap V2)
BSC_RPC_URL=https://bsc-dataseed.binance.org/
BSC_CHAIN_ID=56
BSC_FACTORY=0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73
BSC_WETH=0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c
```

---

### 2.4 Sandbox Agent — 沙盒仿真

**职责**: 管理 Anvil 分叉，执行买卖仿真

**升级点**: 多额度仿真、并发控制

```python
class SandboxAgent(BaseAgent):
    async def run(self, task):
        token = task["token"]
        # 多额度仿真: 小额和大额分别测试
        results = []
        for amount in ["0.1", "1.0"]:
            result = await self.simulator.simulate_buy_sell(token, amount)
            results.append(result)
        # 如果小额能卖但大额不能卖 → 反鲸鱼机制
        return self._merge_results(results)
```

**对应原有代码**: 封装 `services/simulator.py`，增加多额度和并发锁（解决端口冲突 bug）。

目前已知 bug: 同一个代币被检测两次会导致两个 Anvil 同时使用 8545 端口，报 `Address already in use`。需要加 `asyncio.Lock` 或动态分配端口。

---

### 2.5 Auditor Agent — 审计者 (V2 核心)

**职责**: 综合规则引擎 + LLM，做出风险判断

**自主决策能力 (区别于 V1):**

```python
class AuditorAgent(BaseAgent):
    async def run(self, task):
        sim = task["simulation"]
        mode = task.get("mode", "standard")

        if mode == "standard":
            # 标准审计: 规则引擎
            flags = self._evaluate_rules(sim)
            score = self._compute_score(flags)
            return {"flags": flags, "score": score}

        elif mode == "deep_analysis":
            # 🔑 深度分析 (由 Coordinator 决策触发)
            # 1. 获取合约源码
            source = await self.etherscan.get_contract_source(
                task["token"]["address"]
            )
            if source:
                # 2. 用 LLM 分析源码
                llm_result = await self.llm_analyze(source)
                return {"extra_flags": llm_result.flags,
                        "llm_summary": llm_result.summary}
            else:
                return {"extra_flags": ["SOURCE_NOT_VERIFIED"],
                        "llm_summary": "合约未开源，无法进行深度分析"}

    async def llm_analyze(self, source_code: str) -> dict:
        """调用 GLM (智谱AI) API 分析合约源码"""
        prompt = f"""
        你是一个智能合约安全审计专家。请分析以下 Solidity 源码，
        重点关注:
        1. 是否有隐藏的 mint 函数
        2. owner 是否有异常权限
        3. transfer 函数是否有隐藏逻辑（黑名单、暂停、额外扣费）
        4. 是否有可疑的 proxy/delegatecall 模式

        源码:
        ```solidity
        {source_code[:8000]}
        ```
        """
        # 调用 GLM (智谱AI) API (兼容 OpenAI 格式)
        response = await self.client.chat.completions.create(
            model="glm-4-flash",
            messages=[{"role": "user", "content": prompt}],
        )
        return self._parse_llm_response(response)
```

**对应原有代码**: 改造自 `services/analyzer.py` + `services/etherscan.py`。

---

### 2.6 Reporter Agent — 报告者

**职责**: 生成可视化报告 + Chat with Contract 交互

**新增功能: Chat with Contract**

```python
class ReporterAgent(BaseAgent):
    async def chat(self, question: str, context: dict) -> str:
        """
        用户可以对着某个代币的审计结果提问:
        "这个合约的 owner 能不能增发代币？"
        "买入税率 0% 但 ownership 没放弃，安全吗？"
        """
        prompt = f"""
        你是 MemeScan AI 助手。以下是代币 {context['symbol']} 的审计数据:
        - 风险评分: {context['score']}/100
        - 风险标签: {context['flags']}
        - 仿真结果: 可买={context['can_buy']}, 可卖={context['can_sell']}
        - 买入税: {context['buy_tax']}%, 卖出税: {context['sell_tax']}%

        用户问题: {question}
        请用中文简洁回答。
        """
        response = await self.client.chat.completions.create(
            model="glm-4-flash",
            messages=[{"role": "user", "content": prompt}],
        )
        return response.choices[0].message.content
```

**Streamlit 前端** (在 `app.py` 中):
```python
# Chat with Contract 界面
st.subheader("💬 Chat with Contract")
user_question = st.chat_input("输入你的问题...")
if user_question:
    answer = asyncio.run_coroutine_threadsafe(
        reporter_agent.chat(user_question, current_report),
        loop
    ).result(timeout=30)
    st.chat_message("assistant").write(answer)
```

---

## 三、多链支持详细方案

### 为什么选 BSC？

| 对比项 | Ethereum Mainnet | BSC (BNB Chain) |
|--------|-----------------|-----------------|
| Meme 币新增频率 | ⭐⭐ (V2 衰落) | ⭐⭐⭐⭐⭐ (PancakeSwap 极活跃) |
| DEX Factory 接口 | Uniswap V2 Factory | PancakeSwap V2 Factory (**完全相同**) |
| RPC 费用 | Alchemy 免费版限制多 | 公共 RPC **完全免费** |
| Anvil 分叉 | ✅ 支持 | ✅ 支持 |
| **代码改动量** | 无 | **只改 `.env` 配置** |

### 改动方案

**`core/config.py`** 增加多链配置:

```python
class ChainConfig(BaseModel):
    """单条链的配置"""
    name: str
    rpc_url: str
    chain_id: int
    factory_address: str
    weth_address: str
    router_address: str

class AppSettings(BaseSettings):
    # 保留原有的单链配置 (向后兼容)
    rpc_url: str
    chain_id: int = 1
    # ...

    # 新增: 多链配置列表
    extra_chains: list[ChainConfig] = []
```

**`.env` 新增:**
```env
# ── BSC 链配置 ─────────────────────────
BSC_RPC_URL=https://bsc-dataseed.binance.org/
BSC_CHAIN_ID=56
BSC_FACTORY=0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73
BSC_WETH=0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c
BSC_ROUTER=0x10ED43C718714eb63d5aA57B78B54704E256024E
```

---

## 四、LLM 集成 — GLM (智谱AI)

### 为什么选 GLM？

- **兼容 OpenAI API 格式** — 改一行 `base_url` 就行
- **团队已有 API Key** — 无需额外注册
- **中文能力优秀** — 生成的报告质量高

### 改动方案

**`core/config.py` 新增:**
```python
# LLM 配置
llm_api_key: str = ""                                      # 智谱 API Key
llm_base_url: str = "https://open.bigmodel.cn/api/paas/v4/" # 智谱 GLM
llm_model: str = "glm-4-flash"                              # 推荐模型
```

**`services/analyzer.py` 修改:**
```python
from openai import AsyncOpenAI

# 初始化 LLM 客户端 (兼容 GLM/OpenAI/Kimi)
self.llm_client = AsyncOpenAI(
    api_key=self.settings.llm_api_key,
    base_url=self.settings.llm_base_url,
)
```

> 如果后续想换成 Kimi (Moonshot AI)，只需改 `base_url`:
> ```env
> LLM_BASE_URL=https://api.moonshot.cn/v1
> LLM_MODEL=moonshot-v1-8k
> ```

---

## 五、分工安排 (5 天)

### 开发者 A — Agent 框架 & 多链

| 天数 | 任务 | 产出 | 验收标准 |
|------|------|------|---------|
| **Day 1** | 搭建 Agent 框架 | `agents/base.py`、`agents/coordinator.py`、`agents/scanner.py`、`agents/sandbox.py` | Pipeline 以 Agent 方式运行，效果等同 V1 |
| **Day 2** | BSC 多链支持 | 修改 `config.py`、`monitor.py` | 同时监听 ETH + BSC 两条链 |
| **Day 2** | 修复并发 Bug | `simulator.py` 加锁/动态端口 | 两个代币同时扫描不再报 `Address already in use` |
| **Day 3** | Sandbox Agent 升级 | 多额度仿真 (0.1 ETH + 1 ETH) | 能检测到反鲸鱼机制 |
| **Day 4** | 与 B 联调 | 集成 Auditor Agent + Reporter Agent | 端到端跑通: 扫描 → 仿真 → LLM 审计 → Chat 报告 |
| **Day 5** | 前端完善 + Demo 准备 | 多链切换 UI、Chat UI | 可以给老师 Demo 完整流程 |

### 开发者 B — LLM & 智能审计

| 天数 | 任务 | 产出 | 验收标准 |
|------|------|------|---------|
| **Day 1** | GLM API 集成 | 修改 `config.py`、`analyzer.py` | 手动扫描能返回 LLM 分析摘要 |
| **Day 1** | 配置 LLM Provider | `.env` 增加 LLM 配置 | 可通过 `.env` 切换 GLM/OpenAI/Kimi |
| **Day 2** | Auditor Agent 封装 | `agents/auditor.py` | 标准审计 + 深度分析双模式 |
| **Day 3** | Auditor Agent 自主决策 | 决策逻辑代码 | 能根据风险标签自主决定是否追加源码分析 |
| **Day 3** | Reporter Agent + Chat | `agents/reporter.py` | Chat with Contract 功能可用 |
| **Day 4** | 与 A 联调 | 集成所有 Agent | 多链 + LLM 审计端到端 |
| **Day 5** | 报告模板优化 + Demo | 报告增加 LLM 评语 | 生成的报告包含 AI 分析段落 |

### 共同任务

| 天数 | 任务 | 说明 |
|------|------|------|
| **Day 1** | 约定 Agent 接口 | 统一 `run(task) -> dict` 的 task/result 格式 |
| **Day 4** | 集成联调 | A 的 Scanner/Sandbox + B 的 Auditor/Reporter 联调 |
| **Day 5** | Demo 准备 | 录制演示视频、准备 PPT、文档收尾 |

---

## 六、目录结构 (V2)

```
MemeScan/
├── agents/                    # 🆕 Agent 层
│   ├── __init__.py
│   ├── base.py               # Agent 基类
│   ├── coordinator.py        # 协调者 Agent      (A 负责)
│   ├── scanner.py            # 扫描者 Agent      (A 负责)
│   ├── sandbox.py            # 沙盒 Agent        (A 负责)
│   ├── auditor.py            # 审计者 Agent      (B 负责)
│   └── reporter.py           # 报告者 Agent      (B 负责)
│
├── services/                  # 服务层 (V1 保留，被 Agent 封装)
│   ├── monitor.py            # MonitorService (不变)
│   ├── simulator.py          # SimulationService (A 升级: 加锁 + 动态端口)
│   ├── analyzer.py           # AnalysisService (B 升级: 集成 LLM)
│   ├── etherscan.py          # EtherscanService (不变)
│   └── token_info.py         # TokenInfoService (不变)
│
├── core/                      # 基础设施 (小改)
│   ├── config.py             # 增加多链 + LLM 配置
│   ├── db.py                 # 不变
│   ├── web3_provider.py      # 支持多链 Provider
│   └── logging.py            # 不变
│
├── domain/                    # 领域模型
│   ├── models.py             # 增加 AgentDecision 模型
│   └── db_models.py          # 不变
│
├── app.py                     # Streamlit (增加 Chat UI + 多链切换)
├── .env                       # 增加 BSC + LLM 配置
└── requirements.txt           # 增加 openai 依赖
```

---

## 七、每日时间线

```
Day 1 (2/22) ─────────────────────────────────────────
  上午:
    A: 创建 agents/ 目录，实现 BaseAgent + CoordinatorAgent
    B: 配置 GLM API Key，集成到 config.py + analyzer.py
  下午:
    A: 封装 ScannerAgent + SandboxAgent (包装现有 Service)
    B: 实现 AuditorAgent 基础版 (规则引擎 + LLM 摘要)
  ✅ 验收: 用 Agent 方式跑通一次手动扫描，LLM 摘要出现在报告里

Day 2 (2/23) ─────────────────────────────────────────
  上午:
    A: 添加 BSC 链配置，修改 MonitorService 支持多链
    B: AuditorAgent 深度分析模式 (拉取源码 → 喂给 LLM)
  下午:
    A: 修复 Anvil 端口冲突 Bug (asyncio.Lock 或动态端口)
    B: 实现 ReporterAgent 基础版 (报告生成)
  ✅ 验收: BSC 监控能检测到新代币; Auditor 能分析合约源码

Day 3 (2/24) ─────────────────────────────────────────
  上午:
    A: SandboxAgent 多额度仿真 (0.1 + 1.0 ETH)
    B: AuditorAgent 自主决策逻辑 (根据风险标签追加检查)
  下午:
    A: Coordinator 集成决策链路
    B: Reporter Chat with Contract 功能
  ✅ 验收: Coordinator 能自主决定追加深度分析; Chat 能回答问题

Day 4 (2/25) ─────────────────────────────────────────
  全天联调:
    A+B: 端到端测试 — BSC 监控 → 仿真 → 规则+LLM 审计 → Chat 报告
    修 Bug、调优 Prompt、处理边界情况
  ✅ 验收: 完整流程跑通，无报错

Day 5 (2/26) ─────────────────────────────────────────
  上午:
    A: 前端 UI 完善 — 多链切换、Agent 决策过程可视化
    B: 报告模板优化 — LLM 评语排版、Risk Score 可视化
  下午:
    A+B: Demo 准备 — 录演示视频、写 README V2、准备答辩
  ✅ 验收: 能在 Demo 中展示完整的多智能体审计流程
```

---

## 八、Demo 演示剧本 (建议)

### 流程 (约 5 分钟)

1. **开场** (30s): "MemeScan 是一个多智能体 Memecoin 安全扫描系统"
2. **架构介绍** (1min): 展示 Agent 架构图，解释各 Agent 职责
3. **实时演示** (2min):
   - 启动 BSC 链监控 → 等到新代币 (BSC 上通常几分钟内就有)
   - Scanner Agent 检测到新代币 → Coordinator 分发任务
   - Sandbox Agent 仿真 → Auditor Agent 分析 → 报告自动生成
4. **手动扫描** (1min):
   - 输入一个已知蜜罐地址 → 展示 HIGH_RISK 报告
   - 展示 LLM 分析摘要: "该合约存在隐藏的 mint 函数..."
5. **Chat with Contract** (30s):
   - 问: "这个币安全吗？"
   - AI 回答: "不安全。owner 未放弃权限，存在增发风险..."

### 亮点话术

> "与其让每个用户学会看合约代码，不如让 AI Agent 自主判断。
> 我们的 Auditor Agent 会根据初步检查结果，**自主决定**是否需要调用 LLM 对源码做深度分析——
> 这就像一个有经验的审计师，先快速扫一眼，发现问题再仔细看。"

---

## 九、风险与备选方案

| 风险 | 概率 | 备选方案 |
|------|------|---------|
| GLM API 被限速 | 低 | 切换 Kimi 或 DeepSeek (只改 base_url) |
| BSC RPC 不稳定 | 中 | 备用 RPC: `https://bsc-dataseed1.ninicoin.io/` |
| Anvil 不支持 BSC 分叉 | 极低 | Anvil 支持任何 EVM 链分叉 |
| 5 天做不完全部功能 | 中 | **最低可行版本**: Day 1-2 完成 LLM + BSC 就行，Agent 架构可简化 |
| LLM 分析结果不准确 | 中 | 仅作为参考摘要，风险评分仍以规则引擎为准 |

### 最低可行版本 (如果时间不够)

如果 5 天做不完所有功能，**至少完成这三项**:

1. ✅ GLM LLM 集成 (B 负责, 半天)
2. ✅ BSC 多链支持 (A 负责, 半天)
3. ✅ Agent 基类 + Coordinator 编排 (A 负责, 1 天)

这三项足以让项目从 V1 质变为 V2。
